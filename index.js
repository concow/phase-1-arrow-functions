const divide = function() {
    return (2000 / 100);
}

//function divide() {
//   return (2000 / 100); 
//}

const square = (param) => param * param; //implicit return, 
                                        //only situation doesn't require explicit return with the return keyword

const add = (param1, param2) => param1 + param2;